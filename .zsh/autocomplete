# kubectl
[[ $commands[kubectl] ]] && source <(kubectl completion zsh)
# istioctl
# source ~/.istioctl/tools/_istioctl
# kubebuilder
# source  ~/.zsh/completion/_kubebuilder
# eksctl
# fpath=($fpath ~/.zsh/completion)
# autoload -U compinit
# compinit

